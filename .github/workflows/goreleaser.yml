# name: goreleaser

# # ref. https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions
# on:
#   push:
#     tags:
#       - "*"

# permissions:
#   contents: write

# jobs:
#   release:
#     # https://docs.github.com/en/actions/writing-workflows/workflow-syntax-for-github-actions#jobsjob_idstrategymatrix
#     strategy:
#       matrix:
#         job:
#           - os: ubuntu-22.04
#             platform: linux
#             target: gpuhealth_deb_amd64
#             goreleaser_suffix: ""

#           - os: ubuntu-22.04
#             platform: linux
#             target: gpuhealth_deb_arm64
#             goreleaser_suffix: ""

#           - os: ubuntu-22.04
#             platform: linux
#             target: gpuhealth_rpm_x86_64
#             goreleaser_suffix: ""

#           - os: ubuntu-22.04
#             platform: linux
#             target: gpuhealth_rpm_aarch64
#             goreleaser_suffix: ""

#     name: Release ${{ matrix.job.target }} (${{ matrix.job.os }})
#     runs-on: ${{ matrix.job.os }}

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Install OS dependencies
#         shell: bash
#         run: |
#           case ${{ matrix.job.target }} in
#             linux_arm64) sudo apt-get -y update ; sudo apt-get -y install gcc-aarch64-linux-gnu ;;
#           esac

#       - name: Set up Go
#         uses: actions/setup-go@v5
#         with:
#           go-version-file: go.mod

#       - name: Show version information
#         shell: bash
#         run: |
#           gcc --version || true
#           go version

#       # https://github.com/goreleaser/goreleaser-action
#       - name: Run GoReleaser
#         uses: goreleaser/goreleaser-action@v6
#         with:
#           distribution: goreleaser
#           version: latest
#           args: release --config .goreleaser_${{ matrix.job.target }}.yaml
#           workdir: .
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#       - name: Get tag version
#         if: startsWith(github.ref, 'refs/tags/')
#         id: get_tag_version
#         run: echo TAG_VERSION=${GITHUB_REF/refs\/tags\//} >> $GITHUB_OUTPUT

#       - name: Release latest
#         uses: softprops/action-gh-release@v1
#         if: ${{ github.ref == 'refs/heads/main' }}
#         with:
#           name: Latest release
#           tag_name: latest
#           draft: false
#           prerelease: false
#           body: Latest builds from the last commit
#           files: |
#             ./dist/gpuhealth_*.deb
#             ./dist/gpuhealth_*.rpm
