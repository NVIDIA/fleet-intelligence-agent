✓ GPU Health Agent deployed successfully!

NAMESPACE: {{ .Release.Namespace }}
DEPLOY_ENV: {{ .Values.deployEnv }}
{{- if .Values.enroll.enabled }}
ENROLLMENT: Enabled
{{- if eq .Values.deployEnv "prod" }}
Open https://gpuhealth.ngc.nvidia.com to view your GPU health status.
{{- else }}
Open {{ printf "https://gpuhealth.%s.ngc.nvidia.com" .Values.deployEnv }} to view your GPU health status.
{{- end }}
{{- else }}
ENROLLMENT: Disabled (standalone mode)
{{- end }}

Check status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "gpuhealth-agent.name" . }}
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/name={{ include "gpuhealth-agent.name" . }} --tail=50

Docs: https://github.com/NVIDIA/gpuhealth
